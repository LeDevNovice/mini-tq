{
  "name": "mini-tq",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest --run",
    "test:ui": "vitest",
    "typecheck": "pnpm -r --parallel run typecheck",
    "dev": "echo \"Nothing to dev at root yet\"",
    "verify": "pnpm typecheck && pnpm test"
  },
  "dependencies": {
    "typescript": "^5.9.2",
    "vitest": "^2.1.9"
  }
}
